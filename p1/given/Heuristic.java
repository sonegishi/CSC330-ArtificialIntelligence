/**
 * This defines the interface for all heuristics for all search problems.
 * @author Steven Bogaerts
 */
public interface Heuristic {
    
    public int distance(PuzzleState state);
    
}